# REST Wrapper dependencies
# These are additional dependencies needed for the REST wrapper

# FastAPI and ASGI server
fastapi==0.109.0
uvicorn[standard]==0.27.0

# HTTP client for JSON-RPC forwarding
httpx==0.26.0

# Data validation
pydantic==2.5.3

# Environment configuration
python-dotenv==1.0.0
